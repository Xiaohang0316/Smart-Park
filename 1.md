# 智慧园区数字孪生平台 - 开发Prompt

## 项目目标
开发一个**智慧园区数字孪生可视化Web应用**的原型/Demo,展示园区三维场景与实时数据联动的核心体验。重点是**可视化交互效果**和**产品逻辑闭环**,数据可使用模拟数据(mock data),不需要真实对接IoT设备。

## 技术栈要求
- 前端框架:React + TypeScript
- 三维引擎:Three.js(优先,轻量易上手)或 Cesium(如需真实地理坐标)
- 状态管理:Zustand 或 React Context
- UI组件:Tailwind CSS + shadcn/ui
- 图表:ECharts 或 Recharts(用于数据看板)
- 数据:本地Mock JSON + 模拟实时数据流(setInterval模拟IoT推送)

## 核心场景与功能(按优先级分MVP阶段)

### 第一阶段 MVP(先实现这部分,能跑通核心体验即可)
1. **园区总览三维场景**
   - 渲染一个简化的园区模型:5-8栋楼宇(用Box几何体+贴图即可,不需要精细建模)、道路网络、绿化区块、停车场区域
   - 支持鼠标拖拽旋转/缩放/平移视角
   - 昼夜模式切换(灯光+天空盒变化)

2. **空间钻取导航**
   - 点击某栋楼宇 → 相机飞行动画聚焦到该楼 → 展示该楼楼层列表(侧边栏)
   - 点击楼层 → 显示该楼层户型/房间简化平面图(2D俯视或简化3D)
   - 面包屑导航(园区 > A栋 > 3楼),支持返回上一级

3. **图层叠加系统**
   - 顶部/侧边工具栏,提供图层开关:人流热力图、车流状态、告警点位、绿化图层
   - 每个图层用不同的可视化方式呈现(如热力图用颜色渐变面片,告警点用闪烁标记)

4. **停车场模块**
   - 独立停车场三维/2D场景,车位用色块表示状态(空闲绿色/占用红色/预约黄色/故障灰色)
   - 车位点击后弹出详情卡片(车位编号、状态、剩余时间)
   - 一个简单的"反向寻车"输入框:输入车位号,场景内高亮该车位并显示路径线

5. **数据看板(Dashboard)**
   - 独立页面或侧边浮层,展示关键指标卡片:车位使用率、今日人流、能耗趋势、告警数量
   - 用ECharts做2-3个图表(如24小时人流折线图、各楼宇能耗对比柱状图)

### 第二阶段(MVP跑通后再迭代)
6. **告警与设备点位**
   - 场景中随机分布告警图标(消防、安防、设备故障),点击弹出详情
   - 模拟"实时推送":每隔几秒随机生成一条新告警,列表滚动更新

7. **绿化管理**
   - 绿化区块点击后展示台账信息(树木种类、养护记录等模拟数据)

8. **仿真推演(简化版)**
   - 一个"人流疏散仿真"按钮:点击后场景中显示动态粒子/箭头模拟人群从楼内向出口移动的路径动画(不需要真实物理仿真,视觉表现即可)

9. **AI问答入口**
   - 一个简单的聊天侧边栏,用户输入自然语言问题(如"A栋有多少空闲车位"),前端根据mock数据规则匹配返回答案(可以先做规则匹配,不接大模型API)

## 交互与视觉要求
- 整体色调:科技感深色主题(深蓝/黑底 + 荧光青/蓝色高亮),符合"智慧城市大屏"风格
- 关键数据变化要有动效反馈(数字滚动、图表渐入、告警闪烁)
- 响应式布局至少适配桌面端大屏(1920×1080)和普通笔记本分辨率

## 数据结构建议
请先设计以下Mock数据的TypeScript类型定义:
- `Building`(楼宇):id, name, floors[], position, height, occupancyRate...
- `Floor`(楼层):id, buildingId, floorNumber, rooms[]...
- `ParkingSpot`(车位):id, zone, status, plateNumber?, reservedUntil?...
- `Alert`(告警):id, type, level, position, timestamp, description...
- `SensorData`(传感器数据):type(温湿度/人流/能耗), value, timestamp, location...

## 交付要求
1. 先输出整体项目结构和路由设计,再逐步实现各模块
2. 每完成一个模块,确保可以独立预览效果
3. Mock数据生成逻辑单独封装,方便后续替换为真实API
4. 代码注释清晰,组件拆分合理(场景组件、UI组件、数据层分离)

请先从**第一阶段MVP的项目脚手架 + 园区总览三维场景**开始实现。