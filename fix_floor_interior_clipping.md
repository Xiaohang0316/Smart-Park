# 修复记录: 楼层内部视角办公室穿模到楼外

**日期:** 2026-08-15
**问题:** 进入楼层内部 3D 视角后, 楼板/吊顶穿模到楼宇外壳之外, 办公室家具悬空
**涉及文件:** `src/three/engine.ts` — `buildBuildingInterior()`

## 问题现象

点击楼层钻取进入楼宇内部视角后:

1. 每层楼的楼板和吊顶向西、向北伸出楼体外壳 (A栋伸出 15 × 12 个单位), 从楼外看结构穿模到空中
2. 楼板只覆盖楼体 1/4 面积 (西北象限), 其余 3/4 的办公室家具下方没有楼板, 悬空并可看到楼下

## 为什么会产生这个问题

内部结构组的坐标系约定与楼板/吊顶的摆放方式不一致:

- **内部结构组的原点在楼体角点** (西南角): `group.position = b.position - size/2`, 房间网格坐标 0..w × 0..d 从该角点展开。房间色块、家具、立柱、核心筒全部按此约定摆放 (如 `tint.position.set(rx + rw/2, ...)`)
- **楼板和吊顶只设置了 Y 坐标**: `slab.position.y = slabY + 0.11`, x/z 停留在局部原点 (0, 0)。而 `BoxGeometry(w, 0.22, d)` 以自身中心为原点, 于是楼板以**角点为中心**展开, 只覆盖角点周围 w/2 × d/2 的区域:

```
外壳范围:  角点 .. 角点 + (w, d)          ← 正确
楼板范围:  角点 - (w/2, d/2) .. 角点 + (w/2, d/2)   ← 实际 (只设了 y)
          └── 向西/北伸出外壳 w/2 × d/2 ──┘  └── 东南 3/4 无楼板 ──┘
```

推测成因: 内部结构原本以"楼体中心为原点"设计 (旧注释如此), 后来改为角点原点以配合房间网格正坐标, 房间内容都改了, 唯独楼板和吊顶漏改 x/z。

## 修改方案

`src/three/engine.ts`:

```ts
// 楼板: 摆放到楼体角点坐标系的中心 (w/2, d/2), 覆盖完整楼面
slab.position.set(w / 2, slabY + 0.11, d / 2)

// 吊顶: 同样居中; 同时中心从 -0.12 上移为 -0.06, 使顶面与上一层楼板底面齐平,
// 消除原 0.06 高度间隙(从楼外看层间透光的暗缝)
ceiling.position.set(w / 2, slabY + b.floorHeight - 0.06, d / 2)
```

顺带修正了过时注释: 内部结构以楼体**西南角**(`b.position - size/2`)为局部原点, 而非"楼体中心"。

## 验证

无头 Chrome + 世界坐标 AABB 比对, 对 6 栋楼全部楼层逐一检查:

- 修复前: 每层结构 AABB 超出外壳 15 × 12 个单位 (A栋数据)
- 修复后: **6 栋楼所有楼层的结构 AABB 与外壳完全一致, x/y/z 超出量均为 0**
- `tsc --noEmit` 通过

## 经验教训

- 同一局部坐标系内的所有子对象必须使用同一原点约定; 修改坐标系约定时, 要逐一核对**只设置了部分坐标分量** (`position.y = ...`) 的对象 — 它们最容易漏改

---

# 修复记录(第二轮): 四面外墙 + 俯视户型图切剖

**日期:** 2026-08-15
**问题:** ① 楼层内部四面没有外墙, 房间直接开放到楼外; ② 进入楼层后向上旋转相机想俯视户型图, 视线被本层黑色吊顶和上层黑色楼板挡住
**涉及文件:** `src/three/engine.ts`

## 修改方案

### 1. 四面外墙

墙构建循环(`buildBuildingInterior`)原来只处理 `edgeCount == 2` 的共享边(半高隔断)。现改为:

- **外圈边界**(网格列 0/6、行 0/5 上的单房间边)→ **全高外墙**(`floorHeight`), 向内缩半墙厚 0.08, 不伸出楼体外壳; 放入楼层结构组(各层常显, 从楼外看构成楼体外轮廓)
- **内部共享边**(`edgeCount == 2`)→ 保持半高隔断(1.25), 属当前层内容
- **内部单房间边**(如走廊两侧, 网格坐标 0/6、0/5 之外)→ 不加墙, 保持走廊与房间开放的原有布局
- 顺带修复: 共享边原来被两个房间各建一堵墙(同位置两堵墙 z-fighting 闪烁), 现用 `builtEdges` 集合**去重, 每边只建一堵**

### 2. 俯视切剖(updateInteriorCutaway, 每帧调用)

相机向上旋转时, 根据相机高度逐层切换楼板/吊顶可见性:

| 元素 | 规则 |
| --- | --- |
| 当前层及以下楼板 | 常显(当前层地面 + 楼体基座) |
| 更高楼层楼板 | 相机高于其底面时隐藏 |
| 任意层天花板 | 相机高于其底面时隐藏 |

效果: 相机升到当前层天花板之上 → 吊顶与上方楼板逐层消失, 直接俯视当前层户型图(房间色块/家具/半高隔断); 相机回落时逐层恢复, 从楼外看楼体结构保持完整。`exitInterior` 时恢复全部楼板/吊顶可见性。

### 3. 户型图可读性

- 房间地面色块透明度 0.32 → **0.5**(走廊 0.16 → 0.3), 高亮时 0.75, 俯视时色块分区更清晰
- 地面色块加 `userData.roomId`, 俯视时**点击色块可直接选中房间**

## 验证(无头 Chrome)

- 6 栋楼全部楼层 AABB 与外壳比对: **x/y/z 超出量均为 0**(外墙内缩生效)
- A栋3层外墙: 12 段全高墙(北3/西3/东3/南3), 走廊两侧无墙 ✓
- 切剖三态: 室内眼高(吊顶可见)→ 升过天花板(本层吊顶+上层楼板隐藏, 当前层楼板保留)→ 更高(4/5/6层楼板逐层隐藏) ✓
- 14/14 房间色块带 roomId 可拾取 ✓
- `tsc --noEmit` 通过
- 本项目的无头校验方式: 页面暴露 `window.__parkEngine`, 无头浏览器内遍历场景计算世界 AABB, 与外壳 AABB 比对, 可复用做后续几何回归检查
